# MongoDB
MONGODB_URI=mongodb://mongodb:27017/unistay_db?directConnection=true

# JWT
JWT_ACCESS_SECRET=your_access_token_secret_here
JWT_REFRESH_SECRET=your_refresh_token_secret_here
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=30d

# Bcrypt
BCRYPT_SALT_ROUNDS=12

# Application URL (used in emails)
APP_URL=http://localhost:${PORT}

# Email (Gmail SMTP with App Password)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_gmail@gmail.com
SMTP_PASS=your_gmail_app_password

# Openinary (image storage + transformations)
OPENINARY_URL=http://openinary:3000
BETTER_AUTH_URL=http://localhost:3001
OPENINARY_API_KEY=your_api_key_here

# MinIO (used by Openinary internally)
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin123
MINIO_BUCKET=unistay
MINIO_REGION=us-east-1

# OpenRouter (AI model access)
OPENROUTER_API_KEY=your_openrouter_api_key_here
